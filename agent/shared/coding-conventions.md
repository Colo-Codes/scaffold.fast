# Coding Conventions

- Keep imports at top-level.
- Prefer descriptive names.
- Use strict TypeScript types.
- Avoid inline side effects in render paths.
- Keep route files thin and push domain logic to `src/features`.
